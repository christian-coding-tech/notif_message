# Sidebar overlap UI fix

- [x] Locate sidebar and main container CSS rules in `style.css`.
- [x] Update `style.css` so the sidebar never overlaps main content on desktop widths (z-index and container stacking).
- [x] Ensure sidebar collapsed state margin is correct (desktop).

- [ ] Verify on both `dashboard.html` and `admin.html`.


